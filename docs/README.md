# Documentation

This folder is reserved for extended notes, proofs, and reproduction guides.

- Main paper source: `../Collatz_v8_FINAL.tex`
- Validation script: `../src/validate_prefix_law.py`
- Figures: `../*.png` (frozen outputs)

To build the paper see `../Makefile` and `../README.md#usage`.

Future additions (optional): `proofs/`, `experiments/` notebooks for the 12-cell matrix.
